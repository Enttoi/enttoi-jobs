version: 0.0.{build}
configuration: Release
install:
- cd src
- nuget restore
- cd..
cache: packages -> **\packages.config
build:
  project: src\EnttoiJobs.sln
  parallel: true
  verbosity: minimal
test: off
deploy:
- provider: Environment
  name: EnttoiJobs
  on:
    branch: master
artifacts:
- path: ClientsState\bin\ClientsState.zip
  name: clientstate-deploy-package
  type: WebDeployPackage